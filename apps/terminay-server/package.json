{
  "name": "@terminay/server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Terminay's standalone server composition boundary.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "bin": { "terminay-server": "dist/cli.js" },
  "dependencies": { "@terminay/server-core": "1.0.0", "@terminay/protocol": "1.0.0", "node-pty": "^1.1.0", "npm": "12.0.2", "ws": "^8.21.0", "zod": "^4.3.6" },
  "engines": {
    "node": "24.15.0"
  },
  "scripts": {
    "build": "npm run build --workspace @terminay/server-core && tsc -p tsconfig.json && node ../../scripts/bundled-npm-evidence.mjs dist/bundled-npm-evidence.json && node ./scripts/write-release-integrity-manifest.mjs",
    "typecheck": "npm run build --workspace @terminay/server-core && tsc -p tsconfig.json --noEmit",
    "start": "node dist/cli.js",
    "test": "npm run build && npm run build --workspace @terminay/client-core && node --test test/*.test.mjs"
  }
}
