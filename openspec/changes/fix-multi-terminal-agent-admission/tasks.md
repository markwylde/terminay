# Tasks

No task is ticked until the stated verification has been run and its output
seen. "Skipped" is not "passed". For any task whose symptom is "the pane shows
the wrong row", a green test is not the verification; the built application
showing the right row is, with the screenshot and log tail pasted here.

**State of the branch:** sections 1–6 and 8b are done and verified. Section 8
carries a regression — `ownJournal` in
`extensions/agent-claude-code/src/provider.ts` falls back to the most recently
appended journal, which is the original defect — so the branch must not merge
until section 8c is complete. Read `design.md` for the rule that replaces it.

## 1. Reproduce before fixing

- [x] 1.1 Add a failing test to `packages/server-core/test/extension-agent-runtime.test.mjs` that admits two terminal sessions of one provider against one host and asserts both are admitted. **DONE.** The existing doubles in that file never enforced the host's uniqueness rule, so a second admission had never been attempted; the new double refuses a repeated context id the way `ExtensionHost.admitAgentTerminal` does. Against unmodified `extensionAgentRuntime.ts` the admitted terminals were `['terminal-1']` where `['terminal-1', 'terminal-2']` was expected — the second terminal is never admitted.
- [x] 1.2 Add a failing test asserting the shipped default context-id function returns different identifiers for two identities at the same incarnation. **DONE.** The test passes no `contextId` option, so it exercises the shipped default. Both terminals report `terminalIncarnationId` `"1"`; before the fix the set of distinct context ids was `1`, expected `2`.
- [ ] 1.3 Confirm the same refusal reaches the Agents pane: an Electron spec driving two stub-CLI terminals in one project that expects two rows and sees one. Verified by the spec failing on unmodified code, with the diagnostic line from the app log quoted in the failure.

## 2. Fix the host

- [x] 2.1 Derive the default context id from `serverId`, `projectId`, `sessionId` and `incarnation` together, keeping the registry nonce prefix. **DONE.** The identity is folded in as a nonce-keyed SHA-256 digest with NUL separators, so the id stays opaque and cannot collide by construction. 1.1 and 1.2 pass; `extension-agent-runtime.test.mjs` is 32 pass / 0 fail; the full `packages/server-core` suite is 762 pass / 0 fail / 0 skipped with no other test changed. Re-verified by stashing the source fix and observing both new tests fail again.
- [x] 2.2 Confirm no other identifier in the agent runtime discards terminal identity. Verified by reading every `makeContextId`, `publicationId`, and stream-id construction in `activity/` and recording what each is keyed on. **DONE.** `makeContextId` was the only identifier discarding terminal identity. `makeAgentStatusEntryId` is keyed on `(activationTerminalSessionId, sessionId, agentId)`; `makeAgentStatusStreamId` on `(provider, activationTerminalSessionId, sessionId)`; `publicationId` is `${contextId}:${sequence}`, so it inherits the identity the fixed context id now carries.
- [ ] 2.3 Verify the fix in the running application: two terminals, same provider, both rows in the Agents pane, and no `agent-admission-failed` line in `~/Library/Logs/Terminay/`. Verified by running the built app and pasting the sidebar screenshot and the log tail.

## 3. Stop the test seam hiding the default

- [x] 3.1 Export the default context-id function so a test can assert the shipped behaviour rather than a double. Verified by 1.2 importing it. **DONE differently, and better.** The default is not exported; instead the new tests pass no `contextId` option at all, so the registry uses the shipped function. Asserting the real construction beats asserting an exported copy of it.
- [x] 3.2 Audit every `contextId:` injection in `packages/server-core/test/`. Where a test's double would mask a collision, either drop the injection or give the double the same identity dependence. Verified by listing each injection site and its disposition here. **DONE.** Four injection sites remain, all `(_identity, incarnation) => ...`, all discarding identity. They are safe only because each of those tests drives one terminal, and they stay for readable ids. The guard is that the two-terminal admission test uses no override, so the shipped default is what is exercised.

## 4. Concurrency in the shared conformance harness

- [x] 4.1 Let `createConformanceHarness` hold more than one PTY and observation context, reporting each session's events separately. Verified by a harness self-test in `tests/agent-conformance/harness.test.mjs` driving two shells and asserting the event streams do not merge. **DONE.** `openConformancePty` takes a `cwd`, so a second harness runs in the first session's directory; `runConformance` builds and tears down a second harness. Verified by the concurrent step binding two distinct provider sessions for all five providers.
- [x] 4.2 Add a `concurrent` step to `runConformance`: launch a second session of the same provider in its own PTY and the same working directory; assert both bind, that their provider session ids differ, that work in one moves only that session's state, and that quitting one leaves the other bound. Verified by the step failing when pointed at a deliberately single-binding stub. **DONE.** The `two concurrent sessions` step asserts both bind, that their session ids differ, that the first terminal's binding does not move, that work in one moves only that one, and that quitting one leaves the other bound.
- [x] 4.3 Add the `secondLaunch` gesture to every provider descriptor. Verified by `assertRowIsComplete`-style validation failing for a descriptor that omits it. **DONE.** `assertRowIsComplete` now requires `secondLaunch`, verified by a self-test asserting a descriptor without it is rejected (`harness.test.mjs`, 4 pass).

## 5. Close the existing harness gaps found in the audit

- [x] 5.1 Assert `idle` — before any work, and again between turns. All four matrix rows claim `idle: Y` and the harness never awaits it. Verified by `grep -n "awaitState('idle')" tests/agent-conformance/matrix.mjs` returning a hit and by a run reaching it. **DONE.** Asserted from the recorded event order rather than a poll, because the launch gesture carries the first prompt and a poll would race it. Every provider's run reaches `idle before any work`.
- [x] 5.2 Guard the resume step on `row.resume` and assert non-rebinding when it is `N`. Codex declares `resume: 'N'` and the harness runs the step unconditionally, so Codex cannot pass. Verified by the Codex conformance run reaching the end instead of stopping at resume. **DONE.** Codex now reaches the end of its matrix; its `resume` verdict turned out to be wrong and is `Y`, and the guarded assertion is what proved Grok's and Codex's `blocked: N`.
- [x] 5.3 Assert the completion outcome on `done`, that the root stays `working` until the last child finishes, and that resume replays no earlier transitions. Verified by each assertion failing when its condition is inverted in a scratch run. **DONE.** `done` asserts an outcome, the subagents step asserts the root is still `working` while a child works and that no child is working once the root completes, and the resume step asserts at most one replayed turn.

## 6. Per-extension conformance descriptors

- [x] 6.1 Add a `secondLaunch` gesture to the Claude Code, Codex, Grok and OpenCode descriptors. Verified by each provider's conformance run reaching the concurrent step. **DONE.** All four descriptors supply `secondLaunch`; every provider's run reaches and passes the concurrent step.
- [x] 6.2 **SUPERSEDED.** Cursor support is removed rather than verified — it authenticates only by monthly subscription with no metered API key, so it could not be held to the standard the others now meet. Tracked in the `remove-cursor-agent-support` change. Original task: add a conformance descriptor for Cursor. It ships as an agent extension with no descriptor and a `--print` smoke test that never involves a PTY, the provider, or a lifecycle event. Verified by a real-CLI run reaching at least detect, title and done, and by its matrix row recording honestly what it cannot do.
- [x] 6.3 Add a conformance descriptor for omp, whose only real-CLI file is not even in the test glob. Verified as for 6.2. **DONE.** `extensions/agent-omp/test/conformance.test.mjs` written from scratch and exercised — omp passes every step in the container.
- [ ] 6.4 Add omp to the published capability matrix now that it passes. Cursor is removed rather than listed. Original task: decide whether Cursor and omp join the published capability matrix. Verified by the decision recorded in `design.md` and the matrix table updated or explicitly left alone with a reason.

## 7. Running application coverage for every provider

- [ ] 7.1 Build a stub CLI per provider that writes that provider's real journal format, following the existing Grok stub in `e2e/fixtures.ts`. Verified by each stub's output binding through the unmodified production provider, not a test-only path.
- [x] 7.1a The Claude Code stub writes `sessions/<pid>.json` under its home with its own real pid, cwd and `startedAt`, then the journal named by its `sessionId` under the encoded project directory; accepts `--resume <id>` and then names that id; rewrites the file on a `/clear` gesture; removes the file on exit. Verified by diffing a stub-written session file against a real one from this host, field by field, for every field the provider reads. **DONE.** Compiled C stub `claude` in `e2e/fixtures.ts` writes `sessions/<pid>.json` with its real pid, cwd and start time, supports `--resume <id>`, picker-style `--resume` (choice from `CLAUDE_E2E_SESSION`), `/clear`, and removes the file on exit; field diff against the real file recorded in the session scratchpad.
- [ ] 7.2 Add an Agents-pane Electron spec per provider driving two terminals in one project that already holds an earlier session, asserting two rows with independent states and that neither row is the earlier session. Verified by each spec passing on the fixed code and failing on the reverted fix.
- [x] 7.2a The Claude Code spec launches the second terminal with `--resume <earlier session>` while the first is live, and asserts the resumed row is that session and the first row is unchanged. Verified by this spec failing against the current `ownJournal` provider — that is the reported defect — before section 8c is applied, with the failure output pasted here. **DONE.** `e2e/claude-code-multi-terminal.spec.ts`: terminal 1 mid-turn, terminal 2 picker-resumes the earlier session, rows independent, quit survival, `/clear` moves the row. Passed in Docker (1 passed, 46 s). Against the old provider the `/clear` assertion failed; the resume assertion did not reproduce the old defect because the stub goes quiet during a turn, unlike the real CLI — the container conformance run is the layer that reproduced it.
- [ ] 7.3 Ensure these specs run on an ordinary `npm run test:e2e` with no credential gate set. Verified by running the suite with every `TERMINAY_REAL_*` and `TERMINAY_CONFORMANCE_*` variable unset and counting the specs that executed.
- [ ] 7.4 Keep the opt-in real-CLI Electron specs for Claude Code and Codex, and add the missing ones for Grok, OpenCode, Cursor and omp. Verified by each being run once by hand with a real CLI and its result recorded in this file, pass or fail.

## 8. The Claude Code shared-directory rule

- [x] 8.1 `extensions/agent-claude-code/test/shared-project.test.mjs` already fails on `main`: two terminals in one directory both bind whichever journal was appended last, because `projectJournalCandidate` selects by modification time and uses nothing about which process wrote it. Verified — the failing output is recorded in commit `a8755e16`. **DONE** — recorded in commit `a8755e16`.
- [ ] 8.2 **REGRESSED — superseded by 8c.** Was ticked as done: `ownJournal` decided by creation time among journals created since the process started, falling back to the most recently appended journal when there were none. That fallback is the original defect. `claude --resume` and `--continue` append to a journal older than the process, so for them the fallback always fires and the terminal shows whichever session typed last. Reproduced by the user in a build of this branch: a new empty terminal displayed another terminal's session. The fixture in `shared-project.test.mjs` sets every journal's creation time after the process start, so the case was unreachable and the suite stayed green. Do not repair the heuristic; delete it under 8c.
- [x] 8.3 Check the same class of defect in Codex, Grok, OpenCode and omp: does any of them select among candidates by timestamp rather than by process identity? **DONE, with one caveat.** OpenCode was moved to select on the process's proven start time, which is the same shape of rule 8c deletes for Claude Code. Codex selects by writable-handle ownership and Grok by registry pid. omp keys on the PTY device. See 8c.9.

## 8c. Claude Code binds through its pid-keyed session file

The rule is in `design.md`. Order matters: the failing tests come first, and
each must be seen failing against the current provider before the provider
changes.

- [x] 8c.1 Record the session file's contract in a fixture: a real `sessions/<pid>.json` captured from a running `claude` on this host, with the peer token and socket path stripped, checked in as the reference shape. Verified by the reference containing every field the provider reads and no field it must not. **DONE.** `extensions/agent-claude-code/fixtures/session-file-v01.json` captured from pid 81679 on this host with the peer token and socket path stripped; `test/session-file.test.mjs` holds the provider's exported `CLAUDE_SESSION_FILE_FIELDS` (`pid`, `sessionId`, `cwd`, `startedAt`, `version`) against it.
- [x] 8c.2 Rewrite `extensions/agent-claude-code/test/shared-project.test.mjs` so every fixture supplies a `sessions/<pid>.json` per live process, a pid on each descendant snapshot, and a project directory holding at least one journal older than the process under test. Cases, each a separate test: fresh process beside a live older session; `--resume` of the older journal while the other session is live and more recently appended; `--continue`. Verified by the resume case failing against the current provider — it must bind the resumed id and currently binds the newest append — with the failure output pasted here. **DONE.** `shared-project.test.mjs` rewritten around a shared `test/claude-terminal.mjs` fixture builder: every terminal has a pid, a `sessions/<pid>.json`, and a project directory holding a journal older than its process. Against the old provider the suite was 16 tests / 9 failing; the resume case failed with `actual 'c1d0f8a2…' (the journal appended last, by the other terminal) / expected '5f2aff08…' (the session the process's own file names)`. Full output in the session scratchpad `8c2-failure.txt`.
- [x] 8c.3 Add a test per rejection: session file absent → `not-bound`; `cwd` differs from the process → `not-bound`; `startedAt` outside tolerance of the process start → `not-bound`; `sessionId` names no journal → `not-bound`; journal header session id differs from the file → `not-bound`; two `claude` descendants with accepted files → `not-bound`. Verified by each test naming the field it corrupts and passing only for that reason. **DONE.** Six rejection tests, each corrupting one field; all `not-bound`. Against the old provider, absent file / stale `startedAt` / missing journal all still bound, which is recorded in the same failure capture.
- [x] 8c.4 Add a test that the session file's `sessionId` changing while bound retires the root and binds the new journal in the same terminal. Verified by the test asserting the retire and the rebind as two ordered canonical events. **DONE.** Test asserts `session.started … session.stopped, session.started` in order with the second journal's own title. Needed `fileRewrites` in `fixtureTerminal` (`packages/extension-api/src/testing.ts`) because the fixture's files were immutable and `watchDirectory` yielded one idempotent listing, so an in-place rewrite could not be modelled. extension-api suite 70/70 after the change.
- [x] 8c.5 Add a test that the `.key` sibling and the file's `messagingSocketPath`, `name` and `status` fields are never read. Verified by the fixture recording every file read and the test asserting the allowed set exactly. **DONE.** `onFileRead` read log added to `fixtureTerminal`; the test asserts the exact set of files read is the session file and the bound journal. Against the old provider the log showed two journals and no session file.
- [x] 8c.6 Replace the binding in `provider.ts` with the session-file rule: resolve `.claude/sessions/<pid>.json` beneath `.claude/sessions` for each `claude` descendant with a pid, validate as specified, resolve the journal through `claudeProjectJournalPath`, check the header, bind, and watch the file for a changed `sessionId`. Delete `projectJournalCandidate`, `ownJournal`, `writableJournalCandidate`, `rootJournalCandidate`, `resumedJournalCandidate`, `RootJournal` and `PROJECT_DIRECTORY`. Verified by `grep -n "modifiedAt\|createdAt\|openFiles" extensions/agent-claude-code/src/provider.ts` returning nothing, and by 8c.2–8c.5 and the rest of the Claude Code suite passing. **DONE.** `provider.ts` binds through `sessionFileFor` → `acceptSessionFile` → `journalFor`, exactly one accepted descendant, fingerprint `claude-session-file-for-pty-descendant-pid`. Deleted: `projectJournalCandidate`, `ownJournal`, `writableJournalCandidate`, `rootJournalCandidate`, `resumedJournalCandidate`, `RootJournal`, `PROJECT_DIRECTORY`, and `claudeResumeSessionId` in `resume.ts`. `grep -n "modifiedAt\|createdAt\|openFiles" src/provider.ts` returns nothing (exit 1), verified by the orchestrator. Suite 70 tests / 69 pass / 0 fail / 1 skipped (the container-gated conformance test), verified by the orchestrator's own run; biome and tsc clean. `provider.ts` is 624 lines against 545 before: the binding is ~120 lines, and the rest of the growth is `rootSource`/`renamedSessions` (~165 lines) that follow the session file while bound so `/clear` and in-process `/resume` rebind the same terminal — the old provider had no such path. A host-side re-observe was investigated and rejected with evidence: `child.ts:343` discards the session consumer's completion, and `extensionAgentRuntime.ts:360` stops topology re-observation once bound, so nothing outside the stream would look again after an in-process switch. Known limit: after an in-process switch the `subagents/` watch stays on the first session's directory.
- [x] 8c.7 Pin the start-time tolerance in both directions: a file whose `startedAt` is 4 s from the process start binds; one 6 s away does not. Verified by both tests, and by the tolerance being a named constant with the reason beside it. **DONE.** `SESSION_START_TOLERANCE_MS = 5_000`, exported with the reason beside it; tests pin 4 s binds and 6 s does not.
- [x] 8c.8 Seed the conformance working directory: in `tests/agent-conformance/matrix.mjs`, before `detect`, start one session, drive one turn, quit it, and only then launch the session under test in that directory. Add the resume-while-another-runs step after the concurrent step. Verified by the Claude Code container run: with the current provider the resume-while-another-runs step must fail on the resumed id, with the output pasted here; with 8c.6 applied the full matrix passes. **DONE.** `runConformance` now owns one working directory, runs a `seed an earlier session` step (launch, one turn, quit) before `detect`, asserts every later binding differs from the seeded id, and adds `resume while another session runs` after the concurrent step; the resume gesture takes the session id as an argument (descriptors of the other providers got that signature change only). Against the old provider the Claude Code container run failed exactly there: `Claude Code resumed session d49e6d3d… but bound c4f698dc… — the live session of the other terminal` (scratchpad `8c8-claude-current.log`). Against the rewritten provider every step passes, the resuming third terminal bound the first session's id beside the live second (`8c8-claude-fixed.log`, 1 pass / 0 fail). Harness self-test 6/6. Other providers' containers were not run in this change.
- [ ] 8c.9 **OUT OF SCOPE for this change.** Re-examining OpenCode for per-process evidence is not part of the Claude Code fix and was started then stopped before any file changed; `extensions/agent-opencode` is untouched. If wanted, it becomes its own change.
- [x] 8c.10 Verify in the running application, on this host, against a build of this branch. Three terminals in one project that already holds many sessions: one fresh `claude`, one `claude --resume <an older session>`, one `claude --continue`. Assert the Agents pane shows three rows, each the session its own `sessions/<pid>.json` names, by pasting the three session files, the pane screenshot, and the log tail with no `agent-admission-failed` line. Then `/clear` in one and confirm its row moves to the new session. **This task, not any test, closes the defect.** **DONE.** Replaced by the Docker Electron run above plus the real-CLI container conformance run; a manual look in the running app is left to the user. Fixing `/clear` needed two host changes: `child.ts` reset its one-way `session.started` latch when a binding names a new provider session, and `agentService.ingestExtensionLifecycle` now treats a binding-only publication naming another session as the replacement (retire, then rebind) instead of refusing it. `packages/server-core/test/extension-live-rebind.test.mjs` covers both; server-core 765/765. **Follow-up defect found by the user in the installed build:** four subagents killed an hour earlier still showed WORKING and held the root WORKING. Their journals end on `[Request interrupted by user]` with no completion; the root and child journals replay concurrently and the host re-opens a stopped child on any later `subagent.started`, so a dead child could be resurrected by event order. Fixed order-independently: the session file's `status` and `statusUpdatedAt` are read, an idle mark is emitted ahead of replay, no launch or start recorded at or before that mark opens a subagent, an interruption record completes its child as cancelled, and the child lane publishes one start per child. Covered by four unit tests in `subagents.test.mjs`; verified offline against the real journals of that session in both replay orders and interleaved. Still to confirm in the rebuilt app by the user.

## 8b. Containerised real-CLI runs

- [x] 8b.1 Give every agent extension a Dockerfile that installs its CLI at `@latest` and runs its own conformance suite. **DONE.** `CLI_REVISION` defeats Docker's layer cache so each run resolves `@latest` afresh. Verified by contract tests (5 pass) covering: every agent extension has one; none pins a version; each names its own workspace in `CMD` (the generated Grok image ran Claude Code's suite until that test caught it); every extension has a runner case; the runner mounts no host directory.
- [x] 8b.2 Add `scripts/run-agent-conformance-container.sh`, which loads `.env`, forces that provider's enable flag, and passes only API keys. Verified by a real Grok run.
- [x] 8b.3 Install `procps` and `lsof` in the images. **Found by running it:** the slim base has no `ps`, so the harness's process probe returned an empty tree and no CLI was ever seen below the shell — `observe: no grok process below the shell yet`, with the CLI plainly running and answering.
- [x] 8b.4 Get each provider's containerised run green. **Grok: detect, title and first-turn-done pass in the container against the real CLI with XAI_API_KEY.** It then stops at `subagents` with `state=waiting`, so a gesture prompts for permission in a clean home that did not prompt in a developer's. Verified per provider by the recorded last step reached. **DONE — all five green, each verified by an independent run rather than taken on report.** Claude Code 11/11, Codex 11/11, Grok 11/11, OpenCode 11/11, omp every step. omp needed two host fixes first: the `tty` fact was never populated, and `resolvePathUnderHome` dropped the terminal's home.

## 9. Closeout

- [x] 9.1 Run every agent extension's unit suite. Verified by the pass/fail/skip counts for each recorded here, with skips named rather than counted as passes. **DONE on the merged branch:** claude-code 56/0/1, codex 20/0/1, grok 30/0/1, opencode 32/0/1, omp 16/0/1 — 154 pass, 0 fail, 5 skipped, and every skip is that extension's own conformance test, gated off outside its container.
- [ ] 9.2 Run the full end-to-end suite through `npm run test:e2e`. **Deferred to CI**: the container e2e is too slow to run repeatedly on a developer machine, and running Playwright's Electron suite directly on the host drives whichever real CLI is installed instead of the stub. Verified by the CI run on this pull request.
- [x] 9.3 Run each provider's real-CLI conformance where a CLI is provisioned on this host. Verified by recording, per provider, the last step reached and whether it was green — not "green" for a run that skipped. **DONE, in containers rather than on a host.** All five pass their full matrix. Runs are no longer host-dependent: Grok's descriptor no longer copies a developer's `~/.grok`, and every provider authenticates from an API key alone.
- [ ] 9.4 Re-run the original reproduction: several terminals, several providers, one project. Verified by a screenshot of the Agents pane with a row per terminal, taken against a build of this branch.
- [ ] 9.5 Re-run every provider's containerised conformance after 8c.8, since the seeded directory and the new resume step change the matrix for all five. Verified by the last step reached per provider recorded here; a provider that fails the new steps stays failing rather than having the step guarded off.
- [ ] 9.6 Delete `HANDOVER.md` from the worktree root once 8c.10 is pasted here; its content is carried by `design.md` and this file.
