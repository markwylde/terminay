# AGENTS — completed implementation tasks

Completed plans live here as durable implementation history.

- Keep their original numeric filenames, decisions, and completed checkboxes.
- Do not reopen a historical plan for new scope; create a new task in
  `../tasks/` and link it back where useful.

